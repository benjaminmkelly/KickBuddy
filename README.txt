KickBuddy is a 5-a-side football management app that includes both physical and mental wellbeing features.

INSTALLATION:
In order to install the app onto a web server, the contents of the 'build' folder within the 'kickbuddy' folder must be
copied onto your web server into a folder called app.
Both the 'app' folder and the 'api' folder must be in the same directory.

